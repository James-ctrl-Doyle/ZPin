# 更新日志

## 2.6.0（2026-09-20；基线 = 上游 xland/ScreenCapture@7188cb5）

下面都是本 fork 相对上游的改动。

### 新增

- **设置里可一键以管理员模式重启**：任务管理器这类管理员窗口，普通权限的进程截不了
  （鼠标/键盘注入会被 UIPI 拦掉）。设置-通用里会显示当前是否管理员，非管理员时点
  「以管理员模式重启」走 UAC 确认后重启，新实例就能截了；确认后旧实例自动退出。
  顺带清掉了语言文件里残留的原作者微信文案。
- **开机自启**：设置 → 通用里的那个开关，写当前用户的注册表 `Run` 项。**跟着权限走**：
  程序以管理员模式运行时，开机也以管理员身份启动（注册表的 `Run` 项本身没有提权能力 ——
  写进去的程序开机一律是普通权限 —— 所以命令行里带一个标记，让启动起来的实例自己再提权一次）。
  读状态以注册表为准：你手动删了键、或在任务管理器里禁用了启动项，开关就会同步显示成关闭。
- **托盘菜单新增「关闭快捷键（游戏模式）」**：打游戏前在托盘图标右键勾上它，
  F1/F3 等全局热键全部注销，不会再误触；打完再点一次恢复。状态存进配置（重启保持），
  禁用期间在设置页改快捷键也不会把热键注册回来。截图窗口内的按键（翻历史那对）不受影响。
- **截图历史**：每次截图都留一份「整屏画面 + 截图框」，存在 `temp\shots\` 下。按 `,` / `.`
  （可在设置里改）翻看上一张 / 下一张；翻出来的那张可以**重新裁剪、接着标注**。保留天数可设，默认 3 天。
- **翻页键**：除方向键外，新增可配置的 `,`(上一张) / `.`(下一张)。这两个键是「只在截图窗口内生效」的
  窗口内按键，不注册为系统级热键 —— 不会抢走全系统的逗号和句号。
- **快速保存**：开启后点保存直接落到默认目录（默认「下载」，可改），不再每次弹另存为。
  保存的文件名与截图历史同一套格式：`YYYYMMDD_HHMMSS_mmm.png`（如 20260920_150837_330.png），录屏 mp4 同理。
- **选区边框粗细可调**：设置里可调，拖到 0 就是不要边框。
- **内置 Ling**：`ext/Ling` 直接随仓库提供，clone 下来用 Visual Studio 打开 `ScreenCapture.slnx` 就能编译，
  不需要额外准备依赖。

### 改动

- **文字识别（OCR）换用 Windows 自带引擎**：识别不再依赖外部插件，主程序体积不变，
  也不需要再手动往 `plugin` 目录放那个约 25MB 的 `ImageReader.exe`。
- **二维码识别改为独立提示**：识别到就静默复制内容，然后**立刻退出截图状态**；
  那句"已复制"改由一个独立的小窗继续在原地停留 2 秒（不依赖截图窗口，所以关窗了它还在）。
  不再弹对话框。没识别出来则留在原地，方便调整选区重扫。
- **命令行构建前会先结束正在运行的 ScreenCapture 实例**：两种名字都结束
  （`ScreenCapture.build` 与用户可能从 `_review/` 起的那份）—— 否则两个实例抢 F1 热键，
  回归测试会出现"一半用例过、一半挂"的假故障。
- **拖框时的放大镜不再中途消失**：原来鼠标一按下（开始拖框）它就收起来了，于是"起点能对准、
  终点只能靠感觉"。现在整个拖框过程都跟着光标走，取景框还会自动摆到背离拖动方向的那一侧
  （往左上拖就摆到光标左上方），不会压住正在调的选区。
- **`W` / `A` / `S` / `D` 微调光标**：与方向键完全等价（`W` = `↑`），按一下挪 1 个像素 ——
  人手拖鼠标很难精确到那 1 个像素，配合取景框正好把起点 / 终点对准。框选前和框好后都能用。
  **长图 / 录屏阶段不接管这四个键**：那时它们要留给你正在录的那个程序（游戏里就是方向）。
- **设置页的三个开关统一成"文字状态按钮"**：管理员模式 / 开机自启 / 快速保存 —— 原来都是只认
  图标形状的小按钮，看不出是什么、也看不出当前是开是关。现在直接写着「未开启 / 已开启
  / 已开启（管理员）」，点一下切换；写配置失败时按钮保持原状，一眼能看出没生效。
- **管理员模式做成了可关的开关**：以前只能从普通模式一键提权，进了管理员模式就出不来。现在关掉它
  会以普通权限重新启动（提权进程造不出普通权限的子进程 —— 这里借 explorer 的令牌起新实例），
  开与关两个方向都带二次确认。
- **二次确认改用自绘的确认框**：原来是系统 `MessageBox`，它的浅色方框、系统字体跟这个程序
  （自绘的圆角卡片 + 蓝色强调色）完全不搭。现在是自己的小窗，配色 / 圆角 / 按钮跟设置页同一套；
  模态性靠把设置窗口的输入禁掉实现，不会出现"框还开着又点到别的开关"。
- **工具条合并**：截图阶段的按钮合并成一条（绘图 8 + 撤销重做 2 + 长图/录屏/二维码 3 + 关闭保存复制 3）。
- **数据目录改为 exe 同目录**：配置、临时文件、截图历史、语言文件全部跟着程序走，纯绿色；
  **不再创建 `%appdata%\ScreenCapture`**（升级时会读一次老配置并复制过来，老文件保留不动）。
- **录屏收尾精简为两个按钮**：「丢弃」和「保存」（原来的「存到剪贴板」已去掉，录制中的 `Ctrl+C` 也走保存）。
- **长图收尾与录屏对齐**：工具条改为「贴图」「丢弃」「保存」三个按钮（「存到剪贴板」已去掉，
  保存图标同样换成对勾），长图阶段的 `Ctrl+C` 也走保存。回车 / 双击仍是快速复制，行为不变。
- **语言精简为简体中文 + English**：不再提供繁體中文、Русский、Bahasa Indonesia。
- **停用应用内自动更新**：程序不再联网检查新版本、也不会下载 exe 覆盖自身。
  想升级请自行关注本仓库的 Releases。

### 移除

- **GIF 录屏**：只保留 MP4 输出。

### 修复

- 长图滚动匹配改用「上一次滚动量当剪枝种子」：实测 91ms → 1.5ms，结果与完整遍历严格一致。
- 长图匹配失败时先把步长减半重试，不再直接判定"到底了"（原先会截出半截图）。
- 全屏宿主被激活后会把同样是 topmost 的工具条盖住（z 序问题）→ 统一由 `ToolHost::raiseToolbars()` 顶回来。
- **录屏一开始录制，工具条就"不见了"**：从设置态切到录制态时工具条会变宽（计时器比两个音源按钮宽），
  但只改了尺寸、没重算位置，而摆放规则是"右边缘对齐选区右边缘"，于是整条以左上角为锚点向右长出一截。
  选区贴着屏幕右边或全屏录制时，多出来的那截正好把最右边的「保存」顶到屏幕外 —— 看不见也点不到，
  表现就是"工具条没了、退不出来"。现在改完尺寸会重新定位。
- 单根工具条的摆放（`WinCap::layoutTool`，长图 / 录屏用它）只夹了水平方向、没夹垂直方向，
  选区底边贴近或超出工作区底边时整条会跑到屏幕外；另外拿不到显示器信息时会退回一个空矩形，
  把工具条夹到屏幕左上角。两处都补齐（与 `layoutTools()` 一致）。
- 按 `,` 翻出去之后按 `.` 回不到「这次截图本身」；翻页后工具条停在上一张选区的位置（要等鼠标移动才跟过去）。
- 截图标注的删除操作原先会误删第一个图形。
- **LICENSE 的版权主体写错了**：上游换协议时把 ThorVG 项目的 MIT 模板整份抄了过来、版权行忘了改，
  于是许可证里写着「Copyright (c) 2020 - 2026 ThorVG Project」—— 而本项目与 ThorVG 没有任何关系
  （源码、工程文件、exe 里都没有它的引用）。已改成本项目真正的版权人：
  `Copyright (c) 2022 - 2026 LiuXiaoLun (xland)` + `Copyright (c) 2026 James-ctrl-Doyle`。
- `Lang::getSupportedLang()` 会把内置语言与 `Lang` 目录里的文件重复列出（语言菜单出现两条"简体中文"）。
