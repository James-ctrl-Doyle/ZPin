#include "pch.h"
#include "Update.h"

// 自动升级：已停用。
//
// 上游版本会去作者自己的服务端比对版本号、下载新版 exe 覆盖自身。fork 之后这条链路
// 指的是原作者的服务，继续用等于白占对方的流量，而且用户点一下"更新"就会被换成
// 另一个来源的 exe —— 对一个自己维护的 fork 来说这是明确不该有的行为，所以整段下线。
//
// 只保留 Update::checkLater() 这个空实现，让 App.cpp / WinCap.cpp / WinPin.cpp
// 三处调用点不必改动。以后想恢复自动升级：把网络请求这段写回来，地址指向自己的发布地址，
// 并自己托管一份形如 {"version":[2,6,0]} 的版本清单（原来的那份是随上游一起删掉的）。
void Update::checkLater()
{
}
